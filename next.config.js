// This project now uses a custom build script (build.js) for GitHub Pages deployment
// The Next.js config is no longer needed
console.log('Note: This project uses a static build approach. See README.md for deployment instructions.');
